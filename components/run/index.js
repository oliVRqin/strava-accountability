import styles from '../../styles/Home.module.css'
import Running from '../../public/light/running_strava.png'

const Run = ({ data }) => {
    return (
        <p>Running!</p>
    )
}

export default Run